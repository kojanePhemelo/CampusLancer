<%- include('partials/header', { user: user, title: 'Applicants' }) %>

<div class="page-hero">
  <div class="page-hero-inner">
    <h1><%= task.title %></h1>
    <p><%= applicants.length %> applicant<%= applicants.length !== 1 ? 's' : '' %> · Ranked by AI skill score</p>
  </div>
</div>

<div class="page">
  <a href="/business/dashboard" style="font-size:0.85rem;color:var(--gray-600);display:inline-flex;align-items:center;gap:0.3rem;margin-bottom:2rem">← Back to dashboard</a>

  <% if (applicants.length === 0) { %>
    <div class="empty">
      <div class="empty-icon">👥</div>
      <h3>No applicants yet</h3>
      <p>Students will appear here once they apply to this task.</p>
    </div>
  <% } else { %>
    <div class="grid-auto fade-up">
      <% applicants.forEach((a, i) => { %>
        <div class="card card-hover">
          <div style="display:flex;align-items:center;gap:0.85rem;margin-bottom:1rem">
            <div style="position:relative">
              <div style="width:48px;height:48px;border-radius:50%;background:var(--blue-100);display:flex;align-items:center;justify-content:center;font-family:var(--font-display);font-weight:800;color:var(--blue-700);font-size:1rem">
                <%= a.first_name[0] %><%= a.last_name[0] %>
              </div>
              <% if (i === 0) { %>
                <span style="position:absolute;top:-6px;right:-6px;background:#f59e0b;color:#fff;border-radius:50%;width:18px;height:18px;font-size:0.65rem;display:flex;align-items:center;justify-content:center;font-weight:700">★</span>
              <% } %>
            </div>
            <div style="flex:1">
              <div style="font-weight:600;font-family:var(--font-display);color:var(--blue-900)"><%= a.first_name %> <%= a.last_name %></div>
              <div style="font-size:0.8rem;color:var(--gray-600)"><%= a.institution %></div>
            </div>
            <div style="text-align:right">
              <div style="font-family:var(--font-display);font-size:1.3rem;font-weight:800;color:var(--blue-600)"><%= a.ai_skill_score %></div>
              <div style="font-size:0.7rem;color:var(--gray-400)">score</div>
            </div>
          </div>

          <% if (a.cover_note) { %>
            <p style="font-size:0.85rem;color:var(--gray-600);line-height:1.55;background:var(--gray-50);padding:0.75rem;border-radius:var(--radius-md);margin-bottom:1rem;border-left:3px solid var(--blue-300)">
              "<%= a.cover_note.substring(0, 140) %><%= a.cover_note.length > 140 ? '...' : '' %>"
            </p>
          <% } %>

          <div style="display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:0.5rem">
            <div style="display:flex;gap:0.5rem;align-items:center">
              <% if (a.github_username) { %>
                <a href="https://github.com/<%= a.github_username %>" target="_blank" class="btn btn-ghost btn-sm">GitHub</a>
              <% } %>
              <span class="badge badge-green">Accepted</span>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  <% } %>
</div>

<%- include('partials/footer') %>
